#include <stdio.h>
/**
 * main: Entry point
 * Description: 'the programs description'
 * short Description: 'the function is the main'
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
